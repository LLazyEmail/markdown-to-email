import { 
    PlainCallbacks, newLine 
  } from 'atherdon-callbacks';
  
  const {
    strong,
    link,
    blockquote,
    mem,
    header,
    italic,
    del,
    q,
    code,
    hr,
    empty
  } = PlainCallbacks;
  
  const {
    ulList,
    olList
  } = PlainCallbacks;
  
  const {
    image,
    paragraphWrapper,
    sponsorship,
    br
  } = PlainCallbacks;
  
  const { previewText, separator } = PlainCallbacks;
  // ---------------------------------
  
  
  